import 'package:flutter/material.dart';

class CreatesScreen extends StatelessWidget {
  const CreatesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
