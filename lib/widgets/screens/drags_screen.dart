import 'package:flutter/material.dart';

class DragsScreen extends StatelessWidget {
  const DragsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
